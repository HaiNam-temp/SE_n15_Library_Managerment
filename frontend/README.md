React frontend scaffold for Library Manager

Commands (from frontend folder):

# install deps
npm install

# dev server (Vite)
npm run dev

# build
npm run build

# lint
npm run lint

# format
npm run format

Notes:
- Frontend expects backend API at /api (proxy if needed).
- ESLint + Prettier configured for consistent clean code style.
